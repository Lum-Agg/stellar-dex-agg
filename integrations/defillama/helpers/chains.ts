/**
 * Local IDE stub — subset of DefiLlama `helpers/chains`.
 * Upstream: https://github.com/DefiLlama/dimension-adapters/blob/master/helpers/chains.ts
 */

export enum CHAIN {
  STELLAR = "stellar",
}
